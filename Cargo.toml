[package]
name = "rust"
version = "1.6.0"
authors = ["eooce"]
edition = "2021"

[dependencies]
shuttle-runtime = "0.48.0"  # 确保使用最新版本
once_cell = "1.19.0"
hyper = { version = "0.14.9", features = ["full"] }
tokio = { version = "1", features = ["full"] }

[features]
default = ["foo"]
shuttle = ["bar"]
foo = []
bar = []

[[bin]]
name = "rust"
path = "src/main.rs"
